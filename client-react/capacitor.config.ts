import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.radiantiguanablink',
  appName: 'radiant-iguana-blink',
  webDir: 'dist'
};

export default config;
